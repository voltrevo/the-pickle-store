import { FunctionComponent } from "../deps.ts";

const App = (): FunctionComponent => {
  return () => (
    <div>
      <h1>The Pickle Store</h1>
      <p>(I'm not a designer, plz send help)</p>
    </div>
  );
};

export default App;
